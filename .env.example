# Gemini API Key for Live LLM Job Description Parsing, Learning Path Generation, and Code Review
# If GEMINI_API_KEY (or OPENAI_API_KEY) is provided, live LLM requests will be processed dynamically.
# If no API key is set, the application will gracefully fall back to structured mock data.
GEMINI_API_KEY=your_gemini_api_key_here
